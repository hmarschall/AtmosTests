/*---------------------------------------------------------------------------*\
| =========                 |                                                 |
| \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox           |
|  \\    /   O peration     | Version:  1.4                                   |
|   \\  /    A nd           | Web:      http://www.openfoam.org               |
|    \\/     M anipulation  |                                                 |
\*---------------------------------------------------------------------------*/

FoamFile
{
    version         2.0;
    format          ascii;

    root            "";
    case            "";
    instance        "";
    local           "";

    class           dictionary;
    object          fvSchemes;
}

// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

ddtSchemes
{
    default Euler;
}

offCentre 0.5;
SIgravityWaves true;

gradSchemes
{
    default         none;
    grad(theta)     Gauss midPoint;
    grad(u)         Gauss midPoint;
    grad(U)         Gauss midPoint;
    grad((volIntegrate((bf*magSf))&(g|mag(g)))) Gauss midPoint;
}

divSchemes
{
    default             none;
    div(phi,theta)      Gauss linearUpwind grad(theta);
    div(U,theta)      Gauss skewCorrected linearUpwind grad(theta);
    div(U,u)          Gauss skewCorrected linearUpwind grad(u);
    div(phi,U)          Gauss linearUpwind grad(U);
}

laplacianSchemes
{
    default         Gauss linear uncorrected;
}

interpolationSchemes
{
    default                midPoint; 
    interpolate(theta)     midPoint;
    interpolate(grad(theta)) midPoint;
    interpolate(rho)       midPoint;
    interpolate(convection(phi,U)) midPoint;
    interpolate(U)         midPoint;
    interpolate(convection(U,u)) midPoint;
    interpolate(u)         midPoint;
    H                      midPoint;
    interpolate(grad((volIntegrate((bf*magSf))&(g|mag(g))))) midPoint;
    interpolate((volIntegrate((bf*magSf))_0&(g|mag(g)))) midPoint;
}

snGradSchemes
{
    default         none;
    snGrad(Exner)   uncorrected;
    snGrad(theta)   uncorrected;
    snGrad((volIntegrate((bf*magSf))&(g|mag(g)))) uncorrected;
}

fluxRequired
{
    default         no;
    Exner;
}


// ************************************************************************* //
